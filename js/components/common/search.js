/*import articleInfo from "../../utils/articleInfo.js";

export default function search(articles) {
  const search = document.querySelector("input.search");
  const container = document.querySelector(".products-container");

  let articleData = articleInfo;

  articleData;

  search.addEventListener("keyup", doFiltering);

  function doFiltering(event) {
    console.log(event.target.value);

    const searchValue = json.filter(function (article) {
      if (article.title.toLowerCase().includes()) {
        return true;
      }
    });
    console.log(searchValue);
  }*/

  /*  search.onkeyup = function () {
    console.log(event);
    const searchValue = parseFloat(event.target.value.trim());
    console.log(searchValue);

    const filteredArticles = prices.filter(function (article) {
      if (article.title < searchValue) {
        return true;
      }
    });

    products(filteredArticles);
  };
}*/
